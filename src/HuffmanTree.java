import java.io.PrintStream;

public class HuffmanTree {
	public HuffmanTree(int[] count) {
		/**
		 * Constructs a Huffman tree using the given array of
		 *  frequencies where count[i] is the number of occurrences
		 *   of the character with ASCII value i.
		 */
	}
	
	public void write(PrintStream output) {
		/**
		 * Writes the current tree to the given
		 *  output stream in standard format.
		 */
	}
}
